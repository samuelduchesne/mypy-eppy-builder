[project]
name = "{{ package_name }}"
version = "0.1.0"
description = "Type stubs for EnergyPlus {{ eplus_version }}"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Typing :: Stubs Only",
    "Programming Language :: Python :: 3",
]
dependencies = []

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
