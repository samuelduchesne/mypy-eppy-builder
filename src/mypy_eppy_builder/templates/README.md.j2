# eppy-stubs — EnergyPlus {{ energyplus_version }}

Type stubs for `eppy` generated from EnergyPlus {{ energyplus_version }}.

## Install
pip install "eppy-stubs=={{ energyplus_version }}.*"

PEP 561 stub-only package. Install alongside `eppy`; type checkers auto-discover `.pyi`.
See `generated_by.json` for provenance (IDD sha256, timestamp).
